{
  "name": "we-ys",
  "version": "1.4.1",
  "description": "",
  "workspaces": [
    "./app",
    "./server",
    "./externals/*"
  ],
  "scripts": {
    "build": "npm run build:app && npm run build:server",
    "build:app": "cd app && npm run tb",
    "build:server": "cd server && npm run build",
    "dev": "cd app && npm run dev",
    "dev:app": "cd app && npm run dev",
    "dev:server": "cd server && npm run dev"
  }
}